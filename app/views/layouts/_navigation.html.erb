<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#">Fund Transfer</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse"          data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup"   aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <% if user_signed_in?  %>
      <%= link_to t('.add_new'), new_beneficiary_path, class: "nav-link" %>
      <%= link_to t('.list_all'), beneficiaries_path, class: "nav-link" %>
      <%= link_to t('.logout'), destroy_user_session_path, method: :delete, class: "nav-link" %>
      <% end %>
    </div>
  </div>
</nav>
